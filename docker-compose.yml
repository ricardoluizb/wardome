services:
  game:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: wardome-server
    container_name: wardome-server
    ports:
      - "4000:4000"
    ulimits:
      core: -1
